char* strfun(const char* input, int chartype (int));
char** tokenize(const char* input, char** buffer, int chartype(int), const unsigned int len);
char** spacetokenize(const char* input, char** buffer, const unsigned int len);
int isnewline(int character);
